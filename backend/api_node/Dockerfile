#Версия контейнера которая будет взята за основу
FROM node:18

#Обновить данные о версиях приложений
RUN apt update

#Установить удобный командер для навигации по директориям
RUN apt install mc -y

#Внутри контейнера выбираем рабочую папку - где будет располагатся код
WORKDIR /app

#Скопируем код из папки src в контейнер
COPY ./src /app

#Установим зависимости
RUN npm install

#Запустим код - выполнив команду из package.json
CMD ["npm", "start"]